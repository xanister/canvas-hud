import { expect } from 'chai';
import 'mocha';

import { Dialog } from "../src/Dialog";

describe('test', function () {
    let d = new Dialog();

    it('test', function () {

        expect(true).to.be.true;
    });
});